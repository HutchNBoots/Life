DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# IANA timezone all "day" boundaries are pinned to (lib/date.ts). Defaults to
# Europe/London if unset.
APP_TIMEZONE="Europe/London"
